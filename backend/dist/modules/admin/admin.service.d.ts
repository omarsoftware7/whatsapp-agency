import { Repository } from 'typeorm';
import { WebUser } from '../../entities/web-user.entity';
import { Client } from '../../entities/client.entity';
import { CreativeJob } from '../../entities/creative-job.entity';
import { WebPayment } from '../../entities/web-payment.entity';
import { WebReferral } from '../../entities/web-referral.entity';
import { ActivityLog } from '../../entities/activity-log.entity';
export declare class AdminService {
    private userRepo;
    private clientRepo;
    private jobRepo;
    private paymentRepo;
    private refRepo;
    private activityRepo;
    constructor(userRepo: Repository<WebUser>, clientRepo: Repository<Client>, jobRepo: Repository<CreativeJob>, paymentRepo: Repository<WebPayment>, refRepo: Repository<WebReferral>, activityRepo: Repository<ActivityLog>);
    listUsers(): Promise<{
        total_revenue: number;
        referral_count: number;
        id: number;
        email: string;
        password_hash: string;
        google_id: string;
        first_name: string;
        last_name: string;
        avatar_url: string;
        theme_mode: string;
        account_type: string;
        plan_tier: import("../../entities/web-user.entity").PlanTier;
        subscription_status: import("../../entities/web-user.entity").SubscriptionStatus;
        trial_end_at: Date;
        plan_end_at: Date;
        plan_interval: string;
        credits_remaining: number;
        text_credits_remaining: number;
        image_credits_remaining: number;
        video_credits_remaining: number;
        landing_credits_remaining: number;
        credits_reset_at: Date;
        paypal_subscription_id: string;
        payment_provider: string;
        sumit_customer_id: string;
        sumit_recurring_id: string;
        payment_last4: string;
        subscription_started_at: Date;
        last_login_at: Date;
        role: string;
        max_brands: number;
        is_active: boolean;
        created_at: Date;
        updated_at: Date;
        brandLinks: import("../../entities/web-user-client.entity").WebUserClient[];
    }[]>;
    createUser(dto: any): Promise<WebUser>;
    updateUser(userId: number, dto: any): Promise<WebUser | null>;
    listBrands(): Promise<Client[]>;
    listJobs(search?: string, page?: number, limit?: number): Promise<{
        items: CreativeJob[];
        total: number;
        page: number;
        limit: number;
    }>;
    listLandingPages(page?: number, limit?: number): Promise<{
        items: never[];
        total: number;
        page: number;
        limit: number;
    }>;
    getMetrics(periodDays?: number): Promise<{
        active_users: number;
        trial_users: number;
        total_users: number;
        mrr: number;
        arr: number;
        arpu: number;
        total_jobs: number;
        published_jobs: number;
        publishing_rate: string;
        total_referrals: number;
        period_days: number;
    }>;
}
